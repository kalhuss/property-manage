The structure of the technical work:

Tests are in: /Cypress/tests

API routes are found in: src/pages/api

All components are found in: /components

All pages are found in: /pages
Dynamic pages have [] around their name

To run the website: first run npm install -> then run npm run dev to start the server locally

Otherwise it is hosted at: https://property-manage.vercel.app/